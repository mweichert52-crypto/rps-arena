const socket = window.__rpsSocket || (window.__rpsSocket = io()); let role = null; let selected = null; let roomJoined = false; const $ = (id) => document.getElementById(id); const show = (id) => { document.querySelectorAll('.screen').forEach(s => s.classList.remove('active')); $(id)?.classList.add('active'); };